# kome — fish config.
# The bar and the settings hub already show time, load, and status, so the stock
# fish greeting is noise. Set it to an empty string rather than deleting the
# file, so ~/.config/fish stays under kome's installer.
set -g fish_greeting ""
