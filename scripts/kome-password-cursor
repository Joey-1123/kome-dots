#!/usr/bin/env bash
# kome-password-cursor — show password cursor indicator on lock screen

set -euo pipefail

# Get the length of the current password input from hyprlock
# This is a simplified version; hyprlock doesn't expose this directly
# We'll show a subtle indicator

# For now, just return a static indicator
# In a full implementation, you'd parse hyprlock's input state
echo "●"